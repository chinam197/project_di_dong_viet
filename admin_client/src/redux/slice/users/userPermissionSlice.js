import { userPermissions } from "../../middlewares/users/getUserPermission";
import { createSlice } from "@reduxjs/toolkit";
const initialState = {};

const userPermissionsSlice = createSlice({
  name: "userPermission",
});
